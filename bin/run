#!/bin/bash

set -Eeuo pipefail

set +u
source env/bin/activate
set -u

cd meme 
echo "run meme generator"

export FLASK_DEBUG=True

export FLASK_APP=meme

export MEME_SETTINGS=config.py

flask run --host 0.0.0.0 --port 8000

