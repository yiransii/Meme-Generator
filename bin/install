#!/bin/bash

set -Eeuo pipefail
set -x

python3 -m venv env

set +u
source env/bin/activate
set -u

pip install nodeenv
nodeenv --python-virtualenv
deactivate
source env/bin/activate


pip install -e meme 
npm install .