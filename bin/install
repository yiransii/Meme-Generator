set -Eeuo pipefail
set -x

# ======= python 

python3 -m venv env

set +u
source env/bin/activate
set -u

pip install flask

pip install -e meme 

deactivate

# ======javascript 

set +u
source env/bin/activate
set -u

pip install nodeenv
nodeenv --python-virtualenv
deactivate
